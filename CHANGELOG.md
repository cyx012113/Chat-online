# 更新日志

## 7.0.0 - 2026-07-27

### 重大更新

- 在已发布的明文 `6.0.0` 基础上引入强制 TLS 1.3 与端到端加密；协议不兼容旧版，也没有明文回退。
- 应用版本提升到 `7.0.0`，与新的版本化 JSON Lines 安全协议和 E2EE 信封格式明确区分。

### 安全

- 客户端和服务器强制使用 TLS 1.3 与 `chat-online/1` ALPN；客户端通过 TOFU 固定服务器证书 SHA-256 指纹，证书变化时拒绝连接。
- 新增 RSA-3072 长期身份。Windows 私钥使用当前用户的 DPAPI 保护，其他平台使用权限为 `0600` 的 PEM。
- 消息和文件使用随机 AES-256-GCM 内容密钥；内容密钥按收件人使用 RSA-OAEP/SHA-256 包装，信封使用 RSA-PSS/SHA-256 签名。
- 服务器验证身份、签名、路由、当前成员收件人集合和重放 ID，但不解密用户消息、原文件名或文件内容。
- 新增对端 RSA 公钥 TOFU 固定；用户名对应的公钥变化时阻止内容处理。
- 旧版明文历史和附件不会通过新协议中继或下载，避免加密失败后静默降级。

### 客户端

- 新增洛谷风安全 Markdown 渲染：GFM 表格与任务列表、代码高亮、`line-numbers`、`lines=a-b`、引用、链接、分隔线、公式源码，以及对齐、引言和提示容器。
- Markdown 禁用原始 HTML 和危险协议；远程图片与 Bilibili 语法降级为 HTTPS 链接，不自动加载或播放。
- 修复聊天输入区位置与尺寸：编辑区和工具条分层，在 `900x600` 与常用宽屏尺寸下保持稳定。
- 修复服务器和系统消息透明背景，亮暗主题下均使用独立背景、边框和文字颜色。
- 收到实时的他人消息或文件时，若窗口未激活或已最小化，请求系统闪烁提醒；历史加载、自身回显不提醒，并采用 2 秒节流。

### 文件

- 修复 `id` / `file_id` 不一致导致文件气泡下载按钮不可用的问题，并接通气泡下载操作。
- 修复后台读取文件时切换群组可能导致附件发往错误群组的问题；发送前固定群组和收件人快照。
- 新增加密中、发送中、服务器确认、失败、下载与完成状态。
- 原文件名和文件内容在客户端加密；服务器仅保存密文、加密清单、大小和路由所需元数据。
- 单个明文附件上限仍为 8 MiB，存储边界单独计入 AES-GCM 的 16 字节认证标签。

### 服务器与 CLI

- 服务器控制台只允许导出附件密文备份，不会把密文误标为原文件。
- 修复无交互 stdin 提前 EOF 时 headless 服务器立即退出的问题。
- Windows CLI 同时处理 `Ctrl+C` 与新进程组的 `Ctrl+Break`，可可靠停止服务器并完成资源清理。

### 可靠性与发布

- 正确处理 TLS 拆包、粘包、后台线程退出、重连代次、握手中停机和 Windows 对端主动断连。
- 历史与附件使用哈希路径、原子写入、大小限制和 SHA-256 完整性检查。
- Windows x64 发布包包含 GUI、CLI、wheel、完整 sdist、逐文件 SHA-256 清单、第三方许可证和成品冒烟测试。
- 构建产物统一写入 `release/`；构建脚本默认沿用用户的 pip 配置，可按单次构建临时指定软件源。
- 新增密码学、TLS、安全协议、Markdown、存储、服务器、CLI 与 Qt 离屏测试。

### 已知限制

- 用户身份采用“用户名 + 首次所见 RSA 公钥”，TOFU 无法验证第一次连接是否已遭到中间人攻击。
- 静态 RSA E2EE 不提供前向保密；身份私钥日后泄露可能影响该身份过去可解密的已捕获信封。
- 群组密钥按发送时成员封装，后来加入的成员不能解密加入前的历史。
- Windows 可执行文件默认未进行代码签名，首次运行可能触发 SmartScreen。
- Markdown 不支持 `^`/`<` 表格单元格合并、真正的 LaTeX 排版、交互折叠和视频内嵌。

## 6.0.0 - 2026-07-21

### 主要更新

- 使用 PyQt6 重写客户端、服务器控制台和启动器，并拆分网络、协议、存储、主题与复用控件模块。
- 新增主群、邀请码群组、一对一私聊、历史消息、成员角色、禁言、输入状态和自动重连。
- 新增亮色与暗色双主题、现代消息气泡、消息搜索、会话导出、URL 链接和常用表情。
- 新增可视化用户、群组、附件、日志和封禁管理，以及 headless 服务器 CLI。
- 提供 Windows GUI 与 CLI 双入口、便携目录、ZIP、wheel、sdist 和基础自动化测试。

### 安全提示

- `6.0.0` 使用未加密的旧版聊天协议，不包含当前版本的 TLS 1.3、证书固定、RSA 身份或消息和文件 E2EE。
- `6.0.0` 已停止作为当前安全版本维护，且不能与 `7.0.0` 客户端或服务器混用。
